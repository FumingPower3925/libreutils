{
  "name": "@libreutils/tool-text-encoder",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Encode and decode text using various formats (Base64, URL, HTML entities, etc.)",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./meta": "./src/meta.ts"
  },
  "scripts": {
    "dev": "bun run ./src/dev.ts",
    "build": "bun build ./src/index.ts --outdir ./dist --target browser --minify"
  },
  "dependencies": {
    "@libreutils/shared": "file:../../shared"
  }
}